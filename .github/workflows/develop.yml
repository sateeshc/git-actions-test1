name: Java CI with Maven

on:
  push:
    branches: [ "develop" ]
  workflow_dispatch:
    inputs:
      logLevel:
        description: 'Log level'
        required: true
        default: 'warning'

jobs:
  call-workflow-passing-data:
    uses: sateeshc/cicd/.github/workflows/develop.yml@main
    with:
      environment-name: dev
    secrets: inherit
